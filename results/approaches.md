# いま考えられるアプローチの整理（2026-09-22、09-23 に現在地を更新、09-24 に次にやることを §G に整理）

## 0. 現在地（2026-09-23、検査は 09-24。正本は `standing.md` §1）

| | 値 | 出所 |
|---|---|---|
| 全列 対 LASzip | **−16.3%（15/15）**、幅 −37.6〜**−9.1%** | `bench_2m_v21.log` |
| 幾何 / 属性 | **−13.1%**（15/15）/ −18.1%（15/15） | `bench_split_v19.log` |
| 幾何単体 対 LAZ | **17/17**（比の中央値 0.89x） | `bench_three_v21.log` |
| 幾何単体 対 G-PCC | 公称 11/17（0.91x）／土俵を揃えて **15/17 ＋ 2 件未決着** | `bench_three_v21.log`・`data/work/gpcc/gpcc_order.log`・`perm_cheap*.log` |
| 符号化 / 復号 / メモリ | 43.9x / 8.1x / 742 byte/点（復号の比は LASzip の側で揺れる。PCC2 の復号の秒数は v19 から −1.1%） | `bench_2m_v21.log` |

検査（`python/verify_suite.py` 10 項目、`suite_v21.out`・`suite_v22.out`。どれも通過した件数）: 回帰 60/60・
往復と LAS 照合と決定性（符号化し直し）15/15・スレッド数を変えた md5 一致 17/17・次元が全部戻る 15/15・
unpack 15/15・KITTI のバイト一致 108/108・PLY 4/4・候補 290 往復・非可逆 104/104（KITTI 20 frame × 可逆と
誤差上限 4 通りの極座標、PLY 2 件 × 2 のデカルト格子）・命令セットの違う二値との一致 81/81。

2026-09-23 の改善ループで入ったもの: 属性の符号つき文脈・戻りの種類の文脈・
出現回数で速さを変える模型（1 巡目、`losses.md` §27〜§30）、符号つき文脈を旗を読まなかった
幾何の符号器にも（2 巡目、§32）、戻りの種類の文脈を幾何の符号器にも（3 巡目、§33）。外れは 2 つの確率の平均（§31）、選び直しの下限と光の 2 段（§33）。

---

## A0. 別分野から取り込んだもの（2026-09-22）

| 案 | 出どころ | 結果 |
|---|---|---|
| **符号つきの文脈** | JPEG-LS（符号つき勾配） | **入った。**15 件中 **5 件**が動き（AHN3・AHN5・USGS NY・autzen_trim・plane）、残り 10 件は 1 ビットも動かない。幅の下端が −7.6 → **−8.5%**。幾何の中央値は −10.0 → −12.9% だが、これは**中央のファイル（USGS NY）が動いたため**で、全体が 2.9 ポイント良くなったわけではない |
| 縦続の適応フィルタ | MPEG-4 ALS / Monkey's Audio | 外れた（losses.md §22） |
| 文脈ごとの偏り補正 | JPEG-LS / CALIC | 大きく外れた。**外れた理由が符号つき文脈を教えた** |
| 二次推定（SSE / APM） | PAQ / lpaq | **未測定。**次の一手 |
| **パルス構造**（1 パルスの戻りは 1 本の光線に乗る） | 幾何光学・グラフ | **入った。**6/15 件が動き、**中央値 −13.9 → −14.3%**。ただし中央値の動きは**中央の workshop 1 件の変化そのもの**。平行性（cos の中央値 ≈ 1）を予測に使うのは効かず、**続きの点に別の文脈を割り当て、傾きの履歴から外す**ことが効いた。速度の費用はほぼ無い（`structure.md` §1） |
| 曲面による z の予測 | Voronoi / Delaunay の代わり（格子で引く最近傍と平面） | 入れたが **1/15 件で −0.07%**。診断（旗の無い相手）では −6〜15% だった。**符号化の実時間 +19%**（`structure.md` §2）。Delaunay そのものは測っていない |
| ホログラフィー | 光学 | **見込みが低いと判断して未測定**。格子全体の Fourier なら疎なものが密になるが、点の集合の上の変換（グラフ Fourier・RAHT）はそうならないので、結論ではない。位相の巻き戻しにあたる「粗い格子＋残差」は `coldiv` として既にある |
| フレーム間予測 | 映像符号化 | 未測定。KITTI の系列に丸ごと余地 |

詳細は `results/newfields.md`。

## A. 測って効いた、実装すれば入るもの（2026-09-24 時点で 4 案とも決着済み）

**いずれも候補として足せるので、悪化する側は選ばれない。**

| 案 | 効く所 | 当時の実測（試作） | いま |
|---|---|---|---|
| **座標系を回す**（整数せん断・角は副情報） | AHN3、TLS p1 | **−5.0% / −2.5%**（幾何） | **実装済み**（`回転<予測子>`、`architecture.md` §5.2） |
| 照合模型（桁数の繰り返しを旗で送る） | USGS NY | 桁数で −4.5%（幾何の約 −1.3%） | **実装して外した**（22 列で採用 0。`losses.md` §17） |
| 文脈を束ねる（対応表を送る） | extra、vegetation | 桁数で −4.3% / −1.9% | **実装済み**（旗「束」。対応表は送らず観測数で切り替える形） |
| 区間ごとに予測子を選ぶ | workshop、KITTI | −1.5% / −1.0%（幾何） | **実装して外した**（測った全部で負け。`losses.md` §16） |

**合わせても中央値は大きく動かない**（効くファイルが互いに違う）。
だが**幅の下端が上がる** — 下から USGS AK −9.1%、AHN3 −9.8%、USGS NY −10.0% の順（2026-09-23）。
**かつて最下位だった USGS NY は符号つき文脈で −7.6% → −9.7% に上がり、
下端は USGS AK に移った**（`newfields.md`）。**A の 4 案が決着したので、下端に効く案はいま残っていない**
（回転は入った後も AHN3 は −9.8%、照合模型は外した）。§G の S4。

## B. 測って落としたもの（やり直すなら条件を変えること）

| 案 | 結果 | 落ちた理由 |
|---|---|---|
| 内挿（多段の持ち上げ変換） | 10/10 で負け（+6.4〜27.0%） | 格納順が滑らかな曲線でない。八分木の空間的文脈とは軸が違う |
| 予測子の分布を混ぜる | −1.2%〜−0.1% | 適応模型が既に予測の外れを吸収している |
| 高次の文脈をそのまま使う | 桁数 7.3 → 15.6 bpp | 薄まり。**予測として使えば効く**と見て A の照合模型にしたが、それも外した（`losses.md` §17） |
| 重複点を「直前と同じ」の旗で送る | +1.2% | 適応模型が既に最適に近く送っている |
| 軸の入れ替えを回転の前に挟む | −5.0% → +0.9% | 軸ごとの役割（X が最初、Y は X の桁を文脈に）が変わる |
| 度数表の初期値に形のある事前分布 | +1.7〜+7.4% | +32 で速く適応するので外れた事前分布は解くのに費用が要る |
| 面からの z 予測 / 2 次外挿 / 回転予測 | 悪化または 10% 未満 | `losses.md` §9 |

## C. 構造的に閉じているもの

| | 根拠 |
|---|---|
| **対 G-PCC 17/17（公称値）が届かない理由** | 公称値はいま 11/17。**2026-09-23 に緩んだ。**plane は `H(系列) ≤ 5.738` に対しこちらが 5.811 → 5.502 → **5.464** になり、上界を下回った。ただし余裕は 0.274 bpp で決着とは言えない。併用（集合を木で・順序を順位で）は定義上並ぶだけで超えず、自前の集合符号器は 3.360 対 2.775 で **21% 届かない** |
| **順序を捨てる製品** | 全列で 15/15 悪化、中央値 **+25.9%**。属性（gps_time は 6.3 倍）が取得順から意味を得ているため |
| **学習模型** | 復号の再現性が保証できない（方針として採らない）。ただし**概念は算法に移せる**（→ A の 4 件はすべてそれ） |

## D. まだ測っていないもの

| 案 | 見込み | 根拠 |
|---|---|---|
| **属性の同時符号化** | 中〜大 | 強度・分類・戻り番号は互いに強く相関するが、いまは列ごとの符号化に、参照列（最大 4 本の候補から 1 本）との残差と戻りの種類の文脈を足しているだけ。属性は LASzip の bit の中央値 58.5% を占める（`standing.md`）。**§G の S1** |
| ~~属性にも A の 4 案を掛ける~~ | — | **決着。**束は記号版が勝った列なら属性にも既に掛かる。照合模型と区間ごとの予測子は外した |
| 速度の回復 | — | 後置検査を並列にし（§20）、符号つき文脈の候補を実装した経路の勝者に限った（§22）。**比の中央値は分母（LASzip の時間）が揺れるので、費用は PCC2 の実時間で見る。**パルス構造はほぼ費用ゼロ（15 件合計 30.20 → 30.27 s）、**曲面が +19%**（→ 36.16 s、AHN4 _21 4.66 → 6.99 s）。曲面は 1/15 件で 0.07% しか縮まないので、**外す第一候補**である。2026-09-23 の改善ループの 1 巡目でさらに約 +31%（`losses.md` §30 の +14.5% と §29-1 の +14.6%）、2 巡目はほぼ費用ゼロ（+0.4%）、3 巡目 +2.3%、構成の査読の直し（字母・命令セット）で −5.9%（`ab_v19_v21.log`）。選び直しの下限は縮みを失うので付けない（§33）。速い後半での選び直し（+14.6%）はサイズを失わずには軽くできなかった。速さが要るときは `PCC_FAST_RERUN=0` か `PCC_FAST_RERUN_MIN` で外す |
| 非可逆を許す | 中（**−16.2 〜 −52.7%**） | 実装した（`pack --eps`、既定では通らない）。KITTI 20 frame で 2 mm −16.2% / 5 mm −33.8% / 20 mm −52.7%（`lossy_v22.log`。2026-09-23 に誤差を戻した float32 で測るよう直した）。**かつて掲げた −69% は当時の弱い可逆（46.183 bpp）に対する値**で、いまの可逆（21.506 bpp）に対してはこの幅になる。losses.md §21 |
| 論文・公開ページの更新 | — | `standing.html`・`pcc.tex`・`coding.tex` を改善ループ 3 巡目の後の値に合わせ、PDF も作り直した。構成の査読の直しの後に、速度とメモリを v21 の値に合わせ直した（`notes/07_status.md` §4.5）。`pcc.tex` の §e2e も 09-23 に測り直してビット単位で再現した。`order.tex` は測り直さず、測った版（77d3e77 まで）を明記した |

## E. 順序についての見立て（今日いちばん大きい発見）

**点の順序は代償ではなく資産である。**

- 順序を捨てると全列で +25.9%。`gps_time` は 3.066 → 19.168 bpp（6.3 倍）
- G-PCC に幾何で負ける 6 件の理由も、属性で大きく勝っている理由も、**同じ一つの事実**から出る
- 文献が一様に Morton 順へ並べ替えるのは、性能のためだけでなく**空間的な文脈を使えるようにするため**である。こちらはその道を選べない代わりに、属性で取り返している

## F. 測り方について（4 度踏んだ罠）

**見積りの比較相手は、その列で実際に勝っている符号器にすること。**
素朴な方式を相手にすると必ず過大に出る。今日の 4 件:

| | 素朴な相手での見積り | 実際の符号器で測ると |
|---|---|---|
| 交差軸の文脈を細かく | −（標本内エントロピー） | +5.1% |
| 予測子の混合 | −6.9〜−16.7% | −1.2%〜−0.1% |
| 内挿 | −13.0% | +12.8% |
| 回転（plane） | −15.8% | +2.5% |

**回転の AHN3（見積り −9.0% → 実測 −5.0%）だけが残った。**

---

## G. 次にやること（2026-09-24 に整理）

指標の順（可逆を前提に、サイズ → 速度 → メモリ）で並べる。README の「次にやること」はここの要約。

### G-1. サイズ

| # | やること | 見込み | 根拠 |
|---|---|---|---|
| S1 | **属性の同時符号化**（強度・分類・戻り番号を組で送る） | 中〜大・未測定 | 属性は LASzip の bit の中央値 58.5%。いまは列ごとの符号化に、参照列 1 本との残差と戻りの種類の文脈を足しているだけ。見積りの比較相手は、その列でいま勝っている符号器にすること（§F）。下端の USGS AK・NY は属性の割合が 30% 未満なので、下端は上がらない見込み |
| S2 | **推定で切り落とす所の損を測る**（可逆: 幾何の予備選別・参照先の絞り `XKEEP = 2`。非可逆: 格子の選択）。損が出れば実測に替える | 不明 | どれも損を測ったことがない。格子の選択は先頭 20 万点の代理の符号長で決めていて、非可逆だけの話なので可逆の 2 つより後 |
| S3 | **旗の重ね方を全組合せで測る**（いまは貪欲に 3 段） | 小〜中 | 組合せの取りこぼしを測ったことがない（`architecture.md` §7） |
| S4 | 下端のファイル（USGS AK −9.1%、AHN3 −9.8%）に効く案 | 小・案が無い | A の 4 案が決着して、いま下端に効く案は残っていない |
| S5 | 二次推定（SSE / APM） | 未測定 | 別分野から取り込む案として「次の一手」に挙げたまま（§A0） |
| S6 | フレーム間予測（KITTI の系列） | 未測定 | 車載の系列に丸ごと余地（§A0）。いまは frame ごとに独立に符号化している |

### G-2. 速度（サイズを変えないもの）

| # | やること | 根拠 |
|---|---|---|
| V1 | **速 の選び直しで、恒等・素の幅・字母に速を付けた版を試さない** | 勝者にはならない版だが、幾何では予備選別の枠が動きうるので出力が変わらないとは言い切れない（字母の件と同じ構図）。md5 で確かめ、速さの得も測る |
| V2 | **`--sample-select` を直すか外すか** | 既定より遅く、サイズも伸びうる（全点での測り直しが直列） |
| V3 | 面（曲面の予測）の費用の扱い | 入れた時点（改善ループより前の二値）で符号化 +19% に対し 1/15 件で 0.07%（`losses.md` §25）。サイズが第一なので残しているが、費用はいちばん大きい。いまの二値で測り直す |

### G-3. メモリ

| # | やること | 根拠 |
|---|---|---|
| M1 | 走査v1 のメモリを詰める | 幾何だけ・400 万点で G-PCC を 15% 上回る（306 対 266 MB、09-21）。座標を int32 で持つかブロック処理（どちらも器の形に触る） |
| M2 | 全列 742 byte/点 | 全点と候補を同時に抱える設計に由来。削るならサイズとの取引 |

### G-4. 正しさ・再現性

| # | やること | 根拠 |
|---|---|---|
| R1 | **符号化側も libm に依らないようにする** | 復号は機械に依らないが、符号化側は可逆の経路でも libm で計算した値を器に入れる（回転の係数 `asin`・`atan2`・`tan`・`sin`、走査モデルの係数 `atan2`・`sin`、float の格子の検出 `pow`、候補の見積り `log2`）。回転が勝つ AHN3 のようなファイルでは、可逆でも器のバイト列が機械ごとに違いうる。isa の 81/81 は同じ libm の上の比較なので確かめていない |
| R2 | タイル全体（4929 万点）を版 4 の二値で回す | サイズは同じはずだが、200 万点 15 件のバイト一致からの推論にとどまる |
| R3 | x86 以外（ARM など）で確かめる | 命令セットの比較は同じ機械・同じ libm の上 |
| R4 | TLS のデータの単位を確かめる | 記録は cm、値は m として扱っている |

### G-5. 論文

| # | やること | 根拠 |
|---|---|---|
| P1 | `pcc.tex` の要旨に地上型を入れるか | 限界の節は地上型を含むが要旨は含まない。訂正で地上型にも効く結果になった |
| P2 | **地上型の極座標を成果として書く**（取得表現に戻すには正しい中心が要る） | いまは訂正の段落として入っているだけ |
| P3 | `order.tex` を今の二値で測り直すか、版を固定したままにするか | いまは「77d3e77 まで」と明記して固定 |
| P4 | 先行技術の確認（US10796457B2 の独立クレーム、shuffle coding） | `order.tex` の新規性の最大の脅威（`notes/07_status.md` §4） |
| P5 | MPEG の試験系列と Draco での測定 | `order.tex` が試験条件を論じながら、その系列で測っていない |
| P6 | `order.tex` の測定範囲の穴 | 鍵重複 62〜82% のデータが無い・走査v1 は ALS でしか測っていない・副列込みの会計がパイロット 1 件で往復検証を通っていない・4 符号器のうち 2 本が自前で未公刊（`notes/07_status.md` §4） |
| P7 | 幾何の対 G-PCC の未決着 2 件（plane・extra） | plane は上界を 0.274 bpp 下回っただけで決着とは言えない（§C） |

09-20 時点の計画のうち、鍵の縮退の境界（P6 に含めた）と可逆な多重解像度、掃引の分割は
`notes/04_next_steps.md` に履歴として残す。多重解像度と掃引の分割はいまの優先順では後に回した。

### 進める順の提案

1. **R2**（確認だけ）と **V1**（小さい。md5 と速さの得を測る）
2. **S2**（推定の損を測る。小さければ記録、大きければ実測に替える）
3. **S1**（伸びしろが最も大きい）
4. **P2・P1** は S2 の結果が出てからまとめて

