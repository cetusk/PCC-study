# 時系列 — これまでにやったこと

> **履歴**（2026-09-23 の索引 `results/INDEX.md` による）: 09-21 未明までの作業史。**現在の数値は `results/standing.md`**。

証拠は git のコミット時刻と `data/work/` のファイル更新時刻。
節番号は [`results/scan_model_fitting.md`](../results/scan_model_fitting.md) を指す。

---

## 0 期 — 会話が始まる前の蓄積（〜09-19 09:51）

最初のコミット `d50514d` が 178 ファイル・21081 行。ここまでに
正規化レイヤー、PCC2 コンテナ、論文 2 本、94 件の `results/` が既にあった。
以降の作業はすべてこの上に乗っている。

## 1 期 — 土台の整備（09-19 09:51〜12:41）

Apache-2.0 の適用、データ取得の完全化、README と論文の整理、`setup.sh` の修正。
**成果物を人が再現できる形にした**段階で、測定はまだ動いていない。

## 2 期 — 走査モデル符号器、当てはめの誤りを見つけるまで（09-19 16:35〜21:19）

記録 1〜15 節。因果でつなぐとこうなる。

1. 走査モデルを PCC2 の幾何候補として実装して測ったら、**規模で逆転した**
   （200 万点では走査が −5.6%、4929 万点では +10.7%）。
2. 仮説を 2 つ立てて 2 つとも外した（2 節）。成分別に見ると**面外成分が犯人**だった（3 節）。
3. 試作と実装が食い違っていた（4 節）。診断を 3 方向から同時に取った（5 節）。
4. 原因は**ガウス・ニュートンの更新を引き算していた**こと（6 節）。
   毎回逆方向に動いて固着していた。**それまでの数字は全部この実装で出したもの**だった。
5. 直した後（7 節）も残った課題を追い、**レーベンバーグ・マルカート法に作り直した**（9 節）。
6. さらに 2 つの推測を外し（10・11 節）、本当の原因は**分割に失敗した太い走査線**だった（12 節）。
   その「太い線」は走査線の集合ですらなかった（13 節）。回転を実測で選ぶ対処は効かなかった（14 節）。
7. 途中で、**正規化が走査モデルに必要な 3 列を落としていた**ことが判って直した。
8. コードから経緯を書いたコメントを落とした（以後の方針）。
9. バッチから走査モデルを試せない 2 件を外した（`fbd6e14` 21:19）。
   **この除外が後に 29 節の反例になる。** 外した根拠が「鍵が使えない」であり、
   それこそが不変性の条件だったため。

## 3 期 — 一括測定と 5 つの改良（09-19 22:54〜09-20 08:27）

記録 16〜23 節。10 ファイル × 12 構成を通しで測った（**3 時間 34 分**、16 節）。
そこで見つかった欠陥に 5 つ手を入れ、同じ 12 件を測り直した
（**1 時間 42 分**、19 節）。判ったことは 2 つ。

- **掃引の切れ目を定数（200 ulp）で決めてはいけない**（20 節）。
  中央値の 20 倍に変えた。**勝敗を決めているのはここだった**（17 節）。
- **当てはめが割に合うかは採用率で決まる**（21 節）。効かなかったものも記録した（22 節）。

不利な観測も同時に出ている。**LASzip に負けるのが 3 件**（18 節）、
改良の代償として**復号が 56.0 秒から 76.4 秒に悪化**（23 節）。

## 4 期 — 独立査読と、G-PCC との比較（09-20 10:08〜10:48）

- 査読で 9 件の撤回が出た（24 節）。**そのうち 4 件は自分に有利な方向の誤り**
  だったと 27 節が数え直している（「LASzip に負けるのは 0 件」など）。
  24 節は同じものを「結論への肩入れ 1 件」と数えており、**数え方自体が肩入れだった**。
  さらにその数え直しにも誤りがあった（32 節）。**「集計と解釈の誤り」という要約は、
  24 節が使っていた、27 節に否定された言い方である。**
- G-PCC/TMC13 の設定を掃引して記録つきで固定し（25 節）、
  **幾何のみで 10 ファイルを比較**した（26 節）。属性を含めると比較が成立しないため。
- ここで**順序についての観測が崩れて撤回**になった。

## 5 期 — 順序感度（09-20 11:27〜13:24）

- 予備試行で「走査系は置換不変」と観測した（28 節）。
- **これは符号器の性質ではなく鍵が一意であることの性質**だと訂正（29 節）。
  `build_scan_ctx` が (point_source_id, gps_time) で並べ直すので、
  鍵が単射なときに限って不変になる。ただし 30 節の測り直しで、
  実際に効いているのは単射性ではなく完全縮退かどうかだと判った。
- 報告する候補を往復検証に通せるよう `--force-geom` を実装（`cda368d`）。
- **本測定**: 12:04（`cda368d`）以後に起動 → **12:09:52 に COPC の VLR で落ちる**
  → 12:55 / 13:08 / **13:24:55** の 3 分割で測り直して実データが揃う（30 節）。
  12:09:52 から 13:24:55 で **4503 秒＝1 時間 15 分**。
  `order_matrix.txt` の 13:32 は 3 つを結合した時刻であって測定の終端ではない。

## 6 期 — 訂正が波及し、5 期のデータが死ぬ（09-20 13:38〜14:08）

ここが分岐点である。

- **13:38 の `fbdf760` が分岐点である。** ここで Morton の 21 ビット切り捨てを
  直す正規化と、「退避路に落ちた行は失格」の判定が入った
  （`git log -S "np.maximum(b.max(0), 1)"` で確認）。
  **5 期のデータは、揃ってから 14 分後にはもう無効だった。**
- ただし `fbdf760` のメッセージは「`--no-fallback` を実装して測定では退避路を切る」
  と書いているのに、**実際に入ったのは `main.cpp` のフラグだけで、測定スクリプトは
  使っていなかった**（32 節、`21f89c8` に記録）。
  スクリプトに適用されたのは 14:08 の `7c558c2` である。
- 訂正の取り残しを追い（`db9e830`）、撤回した主張が元の節に断定として
  残っていたのを直し（`3789bce`）、**記録中の数値をすべて生ログから
  再生成できるようにした**（`0fe0fc2`、31 節）。
- 実装と記録を突き合わせて、**適用したつもりで適用されていなかった修正**を入れた
  （`7c558c2`、32 節）。置換の成否を検査していなかったのが原因で、
  以後すべての機械的な置換に `assert` を付けている。
- 後から測った結果を元の results に反映し（`0797305`）、測っていない推論と
  撤回済みの法則の断定を直した（`a188746`）。
- **以後の作業はすべて「30 節をもう一度測る」ための準備である。**

## 7 期 — 自己点検を体系にする（09-20 14:16〜14:46）

9 回の自己点検で捉えられなかった誤りが査読で出た（`f03f7d8`）ため、
**点検を場当たりでなく軸として持つ**ことにした。

- 軸を全文の主張抽出に広げ、1〜18 節の数値を監査し、訂正で作った数字にも出所を作った。
- **範囲を限定して「済」にしていた検査**を洗い出して広げた（`dcefaf1`）。
- 注記を付けただけで派生値を直していない箇所を 4 件訂正（`36759e6`）。
- 訂正どうしの食い違い、コードのコメントに残った訂正前の表現まで追った。
- 過去の査読の指摘を**全件照合**し、測定が要らない分は全部閉じた。
- **先行技術を調査**（33 節）。空いているのは「入力順に対する bpp 感度の
  系統的測定」だけだと確認した。
- 誤りの型を測定の記録から**作業の手引き**に移した（`notes/05_verification_checklist.md`）。
- **報告したが記録に書いていなかったもの**を書き残した（`d5ba0da`、型 I）。
- **可逆判定の厳格化が既存の G-PCC 測定を無効にしない**ことを確認（34 節）。
  これで測り直す範囲が PCC2 だけに狭まった。

## 8 期 — 再測の原因 8 件を実装する（09-20 14:54〜15:09）

「記録しただけで放置していた」ものが 8 件あった。放置は見落としである。

- 型 J（限界を記録して対処したつもり）、K（有利な言い換え）、
  L（grep で「動く」と判断）、M（検証済みをコミットせず壊す）を手引きに追加。
- 8 件をコードに実装（複数ブロック化を含む）。複数ブロック化で**再開の仕組みが壊れた**
  のを直した（`8f79add`）。

## 9 期 — 鍵の縮退とディザ（09-20 15:19〜16:59）

- 測定の出力の読み取りを 1 か所に集約（`ordermatrix_io.py`）。
- **鍵の縮退を制御した掃引**（36 節）→ 査読で**全面的に訂正**。
  gps_time が鍵でもあり走査モデルの説明変数でもあるという交絡が主因。
  誤って壊れた文書をコミットしたので型 N を追加。
- **留保に「正直に言うと」のような前置きを付けない**ことを手引きに入れた
  （`d2c2f94` 16:17、型 K）。
- 交絡を切るため**ディザ条件**を実装して測った（37 節）。
  鍵の重複が 100% のとき Δ が 0 に潰れることを確認した。
  ただし**交絡を切れたのは 100% の水準だけで、中間の水準はディザが不発**である
  （量子化幅が float64 の ulp に近く、群を分ける余地が無い）。

## 10 期 — 二度目の起動の前後（09-20 16:59〜17:35）

- 作業規則 7「決めた手順を最後まで通す。途中で次の提案をして方針をずらさない」を追加。
- 最終点検で**LAZ の時間が出力に無い**ことを発見し、列を追加（38 節）。
- 17:27（`7386052`）、「直前に見つけたので 1.6 時間が無駄にならずに済んだ」という
  自分の書き方が有利な言い換えだと指摘され、収支を「既に 1 回失っており、
  2 回目に 3 回目を足さずに済んだ」に直した。
  **直した 1 件を「節約」と書かない**ことを手引きに追加。
- 17:35（`7c6c624`）、**足した列が全行 0.0 になる**ことが判った。LAZ の書き出しが
  1000000 点で 0.047 秒しかかからず、書式が小数 1 桁だったため。
  走り直さず、事実は本文に数値で書く。**列を足したことと、その列が情報を運ぶことは別。**

## 11 期 — 二度目の測定（09-20 17:22〜18:52）

26 ブロック × 8 条件＝204 行。**90 分で完了、検証 NG 0 件。**
集計スクリプトが旧形式のままだったので `ordermatrix_io` を使う形に書き直し、
30 節を差し替えた。

## 12 期 — 論文を書き、8 件の宿題を一括で潰す（09-20 18:00〜）

30 節の測り直しを踏まえて論文（`paper/order.tex`）を書いた。方向は
「可逆点群符号化の比較における順序の扱い」で、33 節が空白と結論した
「入力順のビットレート感度の系統的測定」に当てている。

査読で 8 件の指摘を受けて直した。重いものは 3 つ。

1. **雑音の下限を小さいほうに乗り換えていた。** 逆順対照（±1.6%）を
   種違いの差（0.3%）に置き換えたので、応答するブロックが 6 件から 15 件に
   膨らんでいた。両方を併記し、実効下限で判定し直した。
2. **「鍵が単射かどうかが決めている」を自分のデータが反証していた。**
   plane は鍵重複 82.4% でも +7.0% しか動かない。
3. **単調性 3/22 の 3 件は red-rocks（最大の効果）**で、「雑音と同じ桁だから
   語れない」は事実が逆だった。

さらに「訂正の経緯を論文に書かない」「手法を式で書く」「絶対性能を比較する」
という指示を受けて全面的に書き直した。絶対性能を入れたことで
**「4 符号器は互いに中央値 13% の範囲にいるのに、入力順は同じ点を +122% 動かす」**
という主張が立ち、さらに**順序条件によって勝者が入れ替わる**
（v3 は恒等順で 6 ブロック最短、置換後は 0 ブロック）ことが示せた。

その後、残っていた 8 件（A〜H）を一括で実施した。

- **A** 168 行の厳格判定での再検証 → 本測定の測り直しに吸収
- **B** KITTI・TLS を追加（鍵を持たないので走査モデルは候補から外す）
- **C** 符号器の内部ソート（41 節）
- **D** スライス上限超え（39 節）→ **論文の主張を反証**
- **E** 掃引の対照を 3 本に（42 節）
- **F** 副列込みの会計（全列を候補ごとに 2 列出す）
- **G** 置換の実費用（40 節）→ 基準順を実出力順に直して 7.800 bit/点
- **H** USGS から 2 タイル取得 → **NY の鍵重複 67.0% が、45〜82% の空白を埋めた**

器の拡張には査読をもう一度通した。**最重大の指摘は再開の粒度**で、
1 行でも出ていればブロックを完了扱いにしていたため、8 条件のうち 3 条件で
落ちたブロックが再開時に丸ごと飛ばされ、残りが二度と生成されない状態だった。
条件数で判定するよう直した。

---

## 数の整理

| 量 | 値 |
|---|---|
| コミット | 74（09-19 が 13、09-20 が 61。この文書自身を含む） |
| 記録の節 | 38 |
| 誤りの型 | 14（A〜N） |
| 独立査読 | 6 回（10:08 / 13:38 / 14:16 / 14:34〜14:40 / 15:37。この文書への査読で 7 回目）。35 節の「5 回」は 14:44 時点の値 |
| 30 節の測定 | 一度目 12:09:52〜13:24:55（4503 秒、13:38 に無効化）／二度目 17:22〜18:52（90 分、査読で欠陥）／**三度目 09-21 01:50〜04:23（2.5 時間、17 ファイル 41 ブロック 324 行、確定）** |
| 一括測定 | 一度目 3 時間 34 分（16 節）／再測定 1 時間 42 分（19 節） |

## この時系列から読めること

**手を動かした時間より、測った数字を信用できる状態にする時間のほうが長い。**
09-20 の 61 コミットのうち、新しい測定を足したものは数えるほどで、
残りは訂正・訂正の波及・点検の仕組み作りである。

誤りを一つの形にまとめたくなるが、**記録が名指ししているのは少なくとも 3 つで、
性質が違う。**

1. **実装そのものの誤り。** 2 期のガウス・ニュートンは、足すべきところを
   `th0 - dth` と引いていた（6 節）。手続きの問題ではない。
   記録の「教訓」はさらに別の診断を出している——符号器の内側に代理指標が
   2 つ残っていた（採否を標準偏差で判定／判定の基準が実際の退避路と違う）。
2. **確かめずに次へ進む。** 6 期の「適用したつもりの修正」（型 C・L）、
   10 期の「列を足したが値が出ない」。置換に `assert` を付ける、
   試走の出力を目で見る、といった手当てで閉じられる型である。
3. **推論の誤りと、撤回の方向の偏り。** 少数の点の順序から法則を読んだ箇所が
   4 つあった（17・21・26・30 節、型 A）。中央値が散らばりを隠していた（型 B）。
   そして 27 節が書いているとおり、**撤回した 9 件のうち 4 件は自分に有利な方向**
   であり、24 節はそれを「肩入れ 1 件」と数えていた。数え方が肩入れだった。
   これは手当てで閉じる型ではない。

**3 を 2 に言い換えたくなる引力が常にある。** この文書の初版は 3 件すべてを
2 の形に揃えて書いており、査読で指摘されて直した。17:27 に有利な言い換えの実例を
自分で記録した 11 分後に、同じことをしていた。
