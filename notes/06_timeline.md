# 時系列 — これまでにやったこと

証拠は git のコミット時刻と `data/work/` のファイル更新時刻。
節番号は [`results/scan_model_fitting.md`](../results/scan_model_fitting.md) を指す。

---

## 0 期 — 会話が始まる前の蓄積（〜09-19 09:51）

最初のコミット `d50514d` が 178 ファイル・21081 行。ここまでに
正規化レイヤー、PCC2 コンテナ、論文 2 本、94 件の `results/` が既にあった。
以降の作業はすべてこの上に乗っている。

## 1 期 — 土台の整備（09-19 09:51〜12:41）

Apache-2.0 の適用、データ取得の完全化、README と論文の整理、`setup.sh` の修正。
**成果物を人が再現できる形にした**段階で、測定はまだ動いていない。

## 2 期 — 走査モデル符号器、当てはめの誤りを見つけるまで（09-19 16:35〜21:19）

記録 1〜15 節。因果でつなぐとこうなる。

1. 走査モデルを PCC2 の幾何候補として実装して測ったら、**規模で逆転した**
   （200 万点では走査が −5.6%、4929 万点では +10.7%）。
2. 仮説を 2 つ立てて 2 つとも外した（2 節）。成分別に見ると**面外成分が犯人**だった（3 節）。
3. 試作と実装が食い違っていた（4 節）。診断を 3 方向から同時に取った（5 節）。
4. 原因は**ガウス・ニュートンの更新を引き算していた**こと（6 節）。
   毎回逆方向に動いて固着していた。**それまでの数字は全部この実装で出したもの**だった。
5. 直した後（7 節）も残った課題を追い、**レーベンバーグ・マルカート法に作り直した**（9 節）。
6. さらに 2 つの推測を外し（10・11 節）、本当の原因は**分割に失敗した太い走査線**だった（12 節）。
   その「太い線」は走査線の集合ですらなかった（13 節）。回転を実測で選ぶ対処は効かなかった（14 節）。
7. 途中で、**正規化が走査モデルに必要な 3 列を落としていた**ことが判って直した。
8. コードから経緯を書いたコメントを落とした（以後の方針）。

## 3 期 — 一括測定と 5 つの改良（09-19 22:54〜09-20 08:27）

記録 16〜23 節。10 ファイル × 12 構成を通しで測った（約 1 時間 42 分）。
そこから 5 つの改良を入れて測り直した。判ったことは 2 つ。

- **掃引の切れ目を定数（200 ulp）で決めてはいけない**（20 節）。
  中央値の 20 倍に変えた。**勝敗を決めているのはここだった**（17 節）。
- **当てはめが割に合うかは採用率で決まる**（21 節）。効かなかったものも記録した（22 節）。

## 4 期 — 独立査読と、G-PCC との比較（09-20 10:08〜10:48）

- 査読で**集計と解釈の誤り**が出た（24 節）。以後、査読は 5 回受けている。
- G-PCC/TMC13 の設定を掃引して記録つきで固定し（25 節）、
  **幾何のみで 10 ファイルを比較**した（26 節）。属性を含めると比較が成立しないため。
- ここで**順序についての観測が崩れて撤回**になった。

## 5 期 — 順序感度（09-20 11:27〜13:32）

- 予備試行で「走査系は置換不変」と観測した（28 節）。
- **これは符号器の性質ではなく鍵が一意であることの性質**だと訂正（29 節）。
  `build_scan_ctx` が (point_source_id, gps_time) で並べ直すので、
  鍵が単射なときに限って不変になる。
- 報告する候補を往復検証に通せるよう `--force-geom` を実装（`cda368d`）。
- **本測定**: 12:09 に起動 → COPC の VLR で落ちる → 12:55 / 13:08 / 13:24 の
  3 分割で測り直し → **13:32 にデータが揃う**（30 節）。実測で約 1 時間 15 分。

## 6 期 — 訂正が波及し、5 期のデータが死ぬ（09-20 13:38〜14:08）

ここが分岐点である。

- 査読で 30 節を訂正し、**退避路を切る `--no-fallback`** を入れた（`fbdf760`）。
- 訂正の取り残しを追い（`db9e830`）、撤回した主張が元の節に断定として
  残っていたのを直し（`3789bce`）、**記録中の数値をすべて生ログから
  再生成できるようにした**（`0fe0fc2`、31 節）。
- 実装と記録を突き合わせたら、**適用したつもりで適用されていなかった修正**が出た
  （`7c558c2`、32 節）。Morton の 21 ビット切り捨てもここ。
- **14:07〜14:08 のこの修正群によって、13:32 のデータは使えなくなった。**
  以後の作業はすべて「30 節をもう一度測る」ための準備である。

## 7 期 — 自己点検を体系にする（09-20 14:16〜14:46）

9 回の自己点検で捉えられなかった誤りが査読で出た（`f03f7d8`）ため、
**点検を場当たりでなく軸として持つ**ことにした。

- 軸を全文の主張抽出に広げ、1〜18 節の数値を監査し、訂正で作った数字にも出所を作った。
- **範囲を限定して「済」にしていた検査**を洗い出して広げた（`dcefaf1`）。
- 注記を付けただけで派生値を直していない箇所を 4 件訂正（`36759e6`）。
- 訂正どうしの食い違い、コードのコメントに残った訂正前の表現まで追った。
- 過去の査読の指摘を**全件照合**し、測定が要らない分は全部閉じた。
- **先行技術を調査**（33 節）。空いているのは「入力順に対する bpp 感度の
  系統的測定」だけだと確認した。
- 誤りの型を測定の記録から**作業の手引き**に移した（`notes/05_verification_checklist.md`）。
- **可逆判定の厳格化が既存の G-PCC 測定を無効にしない**ことを確認（34 節）。
  これで測り直す範囲が PCC2 だけに狭まった。

## 8 期 — 再測の原因 8 件を実装する（09-20 14:54〜15:09）

「記録しただけで放置していた」ものが 8 件あった。放置は見落としである。

- 型 J（限界を記録して対処したつもり）、K（有利な言い換え）、
  L（grep で「動く」と判断）、M（検証済みをコミットせず壊す）を手引きに追加。
- 8 件をコードに実装（複数ブロック化を含む）。複数ブロック化で**再開の仕組みが壊れた**
  のを直した（`8f79add`）。

## 9 期 — 鍵の縮退とディザ（09-20 15:19〜16:59）

- 測定の出力の読み取りを 1 か所に集約（`ordermatrix_io.py`）。
- **鍵の縮退を制御した掃引**（36 節）→ 査読で**全面的に訂正**。
  gps_time が鍵でもあり走査モデルの説明変数でもあるという交絡が主因。
  誤って壊れた文書をコミットしたので型 N を追加。
- 交絡を切るため**ディザ条件**を実装して測った（37 節）。
  鍵の重複が 100% のとき Δ が 0 に潰れることを確認した。

## 10 期 — 二度目の起動の直前（09-20 16:59〜17:22）

- 作業規則 7「決めた手順を最後まで通す。途中で次の提案をして方針をずらさない」を追加。
- 最終点検で**LAZ の時間が出力に無い**ことを発見し、列を追加（38 節）。
- 起動後に、**その列が全行 0.0 になる**ことが判った。LAZ の書き出しが
  1000000 点で 0.047 秒しかかからず、書式が小数 1 桁だったため。
  走り直さず、事実は本文に数値で書く。

## 11 期 — 二度目の測定（09-20 17:22〜、進行中）

26 ブロック × 8 条件。見積もり約 1.6 時間。

---

## 数の整理

| 量 | 値 |
|---|---|
| コミット | 73（09-19 が 13、09-20 が 60） |
| 記録の節 | 38 |
| 誤りの型 | 14（A〜N） |
| 独立査読 | 5 回 |
| 30 節の測定 | 一度目 12:09〜13:32（約 1 時間 15 分、使用不能）／二度目 17:22〜（進行中） |

## この時系列から読めること

**手を動かした時間より、測った数字を信用できる状態にする時間のほうが長い。**
09-20 の 60 コミットのうち、新しい測定を足したものは数えるほどで、
残りは訂正・訂正の波及・点検の仕組み作りである。
2 期の「ガウス・ニュートンの符号が逆」も、6 期の「適用したつもりの修正」も、
10 期の「列を足したが値が出ない」も、**実装が間違っていたのではなく、
実装したことを確かめずに次へ進んだ**という同じ形をしている。
